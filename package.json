{
  "name": "oskarrough.com",
  "private": true,
  "scripts": {
    "start": "open src/index.html",
    "build": "rm -rf dist; cp -r src dist && workbox generate:sw"
  },
  "devDependencies": {
    "workbox-cli": "^2.0.1"
  }
}
