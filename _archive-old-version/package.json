{
  "name": "oskarrough.com",
  "private": true,
  "scripts": {
    "start": "open src/index.html",
    "build": "rm -rf dist; cp -r src dist && workbox generateSW workbox-config.js"
  },
  "devDependencies": {
    "workbox-cli": "^3.6.3"
  }
}
